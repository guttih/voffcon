<div class="row">
	<h3 class="page-header pull-right">Trigger actions</h3>
</div>
<h4>Device: "{{item.name}}".</h4>

{{#if errors}}
      {{#each errors}}
        <div class="alert alert-danger">{{msg}}</div>
      {{/each}}
{{/if}}
<div class="row">
	<p> </p>
</div>
<div class="row">
	<div id = "device-list" class="device-list list-group">
	<!-- listItems will be added here-->
	</div>
</div>
{{#if device}} <!-- if you want to edit a specific user -->
			<script type="text/javascript">
			var device = {{{device}}};
		</script>
{{/if}}

<table id="table-triggerAction" class="table table-bordered table-hover">
	<thead>
	</thead>
	<tfoot>
	</tfoot>
	<tbody>
	</tbody>
</table>

<button onclick="window.location.href='/triggeractions/register'" class="btn btn-xs btn-default"> 
	<span class="glyphicon glyphicon-plus"></span>&nbsp;Add Trigger action
</button>

<script src="/js/bootstrap.min.js"></script>	
<script src="/js/list-devicetriggeraction.js"></script>	
